<div class="attaque_message">
    <span class="flavor-text" style="">{{localizeSystem "combat.attaque.label" actor=actor}}</span>
    <div class="dice-formula">
        <ul class="cibles">
            {{#each cibles as |cible|}}
                <li>{{cible.name}}
                    {{#if cible.result}}
                        {{cible.result}}
                    {{else}}
                        {{#if cible.canUpdate}}
                            <span class="button" data-action="reponseAttaque" data-foractoruuid="{{cible.uuid}}">{{localizeSystem "combat.attaque.repondreBtn" }}</span>
                        {{/if}}
                    {{/if}}
                </li>
            {{/each}}
        </ul>
    </div>

    {{valeur}}
    {{#if canLaunch}}
    <div class="button attaque_btn" data-action="deAttaque">{{localizeSystem "combat.attaque.attaqueBtn" }}</div>
    {{/if}}
</div>